import { CompanyOfficerNamePipe } from './company-officer-name.pipe';

describe('CompanyOfficerNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyOfficerNamePipe();
    expect(pipe).toBeTruthy();
  });
});
