import { IncorporationDatePipe } from './incorporation-date.pipe';

describe('IncorporationDatePipe', () => {
  it('create an instance', () => {
    const pipe = new IncorporationDatePipe();
    expect(pipe).toBeTruthy();
  });
});
