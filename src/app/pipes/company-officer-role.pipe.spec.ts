import { CompanyOfficerRolePipe } from './company-officer-role.pipe';

describe('CompanyOfficerRolePipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyOfficerRolePipe();
    expect(pipe).toBeTruthy();
  });
});
